# Base URL for API under test. Example: http://localhost:8080
# If empty or missing, tests will use an embedded WireMock server so they pass out-of-the-box.
# when testing Db assertions leave the baseurl empty
baseUrl=


